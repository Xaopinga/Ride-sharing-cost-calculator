## 现状
- 已在 `index.html:398-400` 绑定“全程距离 (公里)”输入事件：当未手动模式时调用 `autoCalculateTollFee()`。
- `autoCalculateTollFee()` 在 `index.html:407-415` 按“总里程 × 费率”计算并写回 `tollFee`，随后计算收益与图表更新。

## 问题目标
- 当“全程距离 (公里)”输入框内容变化时，高速费应自动按费率计算并更新。

## 拟调整方案
- 方案 A（保留手动覆盖）：保留当前逻辑，仅当用户未手动编辑高速费（无 `data-manual`）时自动计算；符合“自动/手动两种模式”的设计。
- 方案 B（无条件自动）：移除 `data-manual` 判断，任何时候“全程距离”变化都自动计算，始终覆盖高速费。
- 方案 C（增强交互）：保留方案 A，并在高速费输入右侧增加“恢复自动”按钮，一键清除手动标记并立即按费率重算。

## 实施步骤
1. 根据选择的方案修改 `index.html:398-400`：
   - A：维持 `if (!document.getElementById('tollFee').hasAttribute('data-manual')) { autoCalculateTollFee(); }`（无需改动）。
   - B：改为无条件 `autoCalculateTollFee();`。
   - C：维持 A 并在高速费输入旁加入“恢复自动”按钮，点击后移除 `data-manual` 并调用 `autoCalculateTollFee()`。
2. 保持费率联动（已在 `index.html:621-626` 实现，费率滑条/输入变化会触发自动计算）。
3. 回归测试：更改全程距离与费率，观察高速费、利润与图表是否同步刷新；手动输入时是否符合所选方案的覆盖策略。

## 验证标准
- 更改“全程距离”后，高速费按费率自动更新；收益与图表同步变化。
- 在方案 A/C 下，手动输入后不会被自动覆盖，除非“恢复自动”或更改费率触发恢复；在方案 B 下任何变更都自动覆盖。

## 请确认
- 你希望采用方案 A（保留手动覆盖）、方案 B（无条件自动），还是方案 C（保留手动并提供“恢复自动”按钮）？默认我将采用方案 A。